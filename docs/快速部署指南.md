# AI RAG Starter 快速部署指南

## 📋 前置要求

- Node.js 20+
- npm 或 yarn
- DashScope API Key（[获取地址](https://dashscope.console.aliyun.com/apiKey)）

---

## 🚀 本地开发部署

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

```bash
# Windows
copy .env.example .env.local

# Linux/Mac
cp .env.example .env.local
```

编辑 `.env.local` 文件，填入你的 API Key：

```env
DASHSCOPE_API_KEY=your_actual_api_key_here
LLM_PROVIDER=qwen
MODEL_NAME=qwen-max
UPLOAD_DIR=./uploads
VECTOR_STORE_PATH=./.milvus
```

### 3. 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:3000

---

## 🐳 Docker 部署

### 1. 配置环境变量

```bash
copy .env.example .env
```

编辑 `.env` 文件填入 API Key。

### 2. 构建并启动

```bash
docker-compose up -d
```

### 3. 查看日志

```bash
docker-compose logs -f
```

### 4. 停止服务

```bash
docker-compose down
```

---

## 📖 使用说明

1. **上传知识文档**：支持 PDF 和 TXT 格式
2. **提问**：在输入框中输入问题，AI 会基于上传的文档进行回答
3. **多轮对话**：支持连续提问

---

## 🔧 常见问题

### Q: 提示 DASHSCOPE_API_KEY is required
A: 请确保 `.env.local` 文件中配置了正确的 API Key

### Q: 上传文件失败
A: 检查 `uploads` 目录权限，确保应用有写入权限

### Q: Docker 构建失败
A: 确保 Docker 已正确安装并运行

---

## 📁 项目结构

```
ai-rag-starter/
├── src/
│   ├── app/              # Next.js App Router 页面
│   │   ├── api/          # API 路由
│   │   │   ├── chat/     # 聊天接口
│   │   │   └── upload/   # 文件上传接口
│   │   ├── page.tsx      # 主页面
│   │   └── layout.tsx    # 布局组件
│   ├── lib/              # 核心库
│   │   ├── llm.ts        # LLM 配置
│   │   ├── rag.ts        # RAG 检索逻辑
│   │   └── vector-store.ts # 向量存储
│   └── types/            # 类型定义
├── docs/                 # 文档目录
├── uploads/              # 上传文件存储
├── .milvus/              # 向量数据库存储
├── Dockerfile
├── docker-compose.yml
└── package.json
```

